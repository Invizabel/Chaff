## NIST 800-88 Tool

#### To Erase:
* To run the Python code: Python3 -m Chaff
* To run the C code: gcc main.c && ./a.out filenameX filenameY filenameZ

#### Notes:
* On HDD's, you can overwrite any file you want
* On SSD's, you have to overwrite the entire drive
* NIST 800-88 with verify currently only works with the C code

###### Todo:
* Add code to verify the following: can we write? (permissions)(C)
